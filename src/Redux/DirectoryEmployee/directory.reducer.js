import DATA_EMPLOYEE from "../DATA_EMPLOYEE"
const INITIAL_STATE = DATA_EMPLOYEE

const directoryReducer = (state = INITIAL_STATE, action) => {
        switch (action.type) {
                default: return state
        }
}
export default directoryReducer